# GenLayer Bradbury Testnet
# This project uses the GenLayer Bradbury testnet for wallet connectivity.
# No API keys are needed. The frontend communicates directly with the chain via RPC.
NEXT_PUBLIC_CONTRACT_ADDRESS=
NEXT_PUBLIC_CONTRACT_DEPLOY_TX=
